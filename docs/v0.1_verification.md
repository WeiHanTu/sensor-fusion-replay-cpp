# v0.1 Verification Status

Status date: 2026-09-09. This is an incomplete release record, not a release
announcement. `v0.1.0` MUST NOT be tagged from the current evidence.

## Verified implementation evidence

| Check | Result | Boundary |
| --- | --- | --- |
| macOS debug tests | 73/73 passed | AppleClang 16, OpenCV 5.0 |
| macOS release tests | 73/73 passed | Fresh Release configure of clean implementation commit `7432516` |
| Ubuntu GCC tests | 73/73 passed | Exact `7432516` source archive, GCC 13, OpenCV 4.6, Ubuntu 24.04 local container |
| Ubuntu sanitizer tests | 73/73 passed | Exact `7432516` source archive, Clang 18 ASan+UBSan, Ubuntu 24.04 local container |
| `format-check` | Passed | 31 project C++ files in the configured target list |
| `clang-tidy` | Passed | Target completed with no actionable project diagnostics; dependency/system warnings were suppressed, so this is not a zero-warning claim |
| Synthetic CLI integration | Passed | Authored temporary fixtures only; no private KITTI data |
| Projection benchmark | Passed | Clean implementation commit and exact authored workload described below |

The Ubuntu runs are local container evidence, not hosted GitHub Actions evidence.

## Retained projection benchmark

- Report: [`results/projection_benchmark_apple_m4_pro.json`](results/projection_benchmark_apple_m4_pro.json)
- SHA-256: `9eaa8d86063020b3aacb100b9f36eeea894baeb7676a407ef42534d514acb322`
- Schema: `1.2.0`
- Implementation commit: `743251641dec3c21c6a5ba396f9f83c938f754f3`
- Build: clean `Release`, AppleClang 16.0.0.16000026
- Host: Darwin 25.6.0, arm64, Apple M4 Pro
- Workload: 100,000 authored deterministic finite points per sample, 10 warm-up
  iterations, 100 measured iterations
- Latency: min `0.525 ms`, mean `0.583 ms`, p50 `0.583 ms`, p95 `0.629 ms`,
  p99 `0.670 ms`, max `0.678 ms`
- Aggregate throughput: `171.4 million points/s`; rate at p50 latency:
  `171.4 million points/s`
- Accounting per iteration: 70,000 visible + 15,000 behind/near + 15,000
  outside image = 100,000 input; all 10 warm-up and 100 measured iterations
  were checked
- Percentiles: nearest-rank

This measures SE(3) transformation, rectified projection, classification, and
output-vector construction. It excludes fixture generation, validation, image
decode, visualization, serialization, queues, synchronization, and replay
pacing. See [`benchmark_walkthrough.md`](benchmark_walkthrough.md).

## Release blockers

- Hosted GitHub Actions has not run because nothing has been pushed.
- No private KITTI image/LiDAR smoke run or private sensitivity run has been
  performed and visually inspected.
- Public synthetic overlay and sensitivity images have not been curated into
  the repository documentation.
- README commands have not been rerun from a fresh clone/check-out.
- No owner-approved push or `v0.1.0` tag exists.

Therefore Phase 1.6's benchmark subtask is verified, but the Phase 1.6 release
gate and the complete `v0.1 Geometry` acceptance gate remain open.
